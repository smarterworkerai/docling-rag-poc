# Diagram sources

GitHub renders these Mermaid blocks natively in the README — no build step, no
pre-rendered assets. Edit the `mermaid` code blocks in `README.md` directly.

(The earlier PlantUML experiment lived here; it was replaced by Mermaid because
GitHub renders it inline with no regeneration step.)
